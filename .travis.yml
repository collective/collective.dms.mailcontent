language: python
python:
  - 2.7

cache:
  directories:
    - buildout-cache

install:
  - mkdir -p buildout-cache/{eggs,downloads}
  - python bootstrap.py -c travis.cfg
  - bin/buildout -c travis.cfg -N annotate
  - bin/buildout -c travis.cfg -Nqt 5
script:
  - bin/test
notifications:
  email:
    - devs@imio.be
    - vincentfretin@ecreall.com
    - cedricmessiant@ecreall.com
